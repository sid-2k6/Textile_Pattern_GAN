# Core (torch/torchvision are preinstalled on Google Colab)
torch>=2.0
torchvision>=0.15
# FID / KID
torchmetrics>=1.0.0
torch-fidelity
scipy
# Data / plotting
numpy
pandas
matplotlib
pillow
scikit-learn
# StyleGAN2 baseline (pure-PyTorch, no custom-CUDA build) + its runtime deps
stylegan2_pytorch==1.8.10
vector_quantize_pytorch
kornia
einops
retry
tqdm
